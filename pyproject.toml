[tool.poetry]
name = "t3"
version = "0.1.0"
description = ""
authors = ["Nikita <zhdan.019@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
tabulate = "^0.9.0"
pytest = "^9.0.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
